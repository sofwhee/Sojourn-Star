"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.collectFeatures = collectFeatures;
var _postcssValueParser = _interopRequireDefault(require("postcss-value-parser"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function (e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
async function collectFeatures({
  container,
  ignoreFeatures
}) {
  const promises = [];
  container.each(node => {
    if (node.type === 'atrule') {
      promises.push(Promise.resolve().then(() => _interopRequireWildcard(require("./at-rules/collect_features"))).then(({
        collectFeatures
      }) => collectFeatures({
        ignoreFeatures,
        node
      })).then(features => {
        return features.filter(f => !ignoreFeatures.has(f.id));
      }));
    }
    if (node.type === 'rule') {
      promises.push(Promise.resolve().then(() => _interopRequireWildcard(require("./selectors/collect_features"))).then(({
        collectFeatures
      }) => collectFeatures({
        ignoreFeatures,
        node
      })).then(features => {
        return features.filter(f => !ignoreFeatures.has(f.id));
      }));
    }
    if (node.type === 'decl') {
      const valueRoot = (0, _postcssValueParser.default)(node.value);
      promises.push(Promise.resolve().then(() => _interopRequireWildcard(require("./properties/collect_features"))).then(({
        collectFeatures
      }) => collectFeatures({
        ignoreFeatures,
        node,
        valueRoot
      })).then(features => {
        return features.filter(f => !ignoreFeatures.has(f.id));
      }));
      promises.push(Promise.resolve().then(() => _interopRequireWildcard(require("./types/collect_features"))).then(({
        collectFeatures
      }) => {
        return collectFeatures({
          nodes: valueRoot.nodes,
          parent: node
        });
      }).then(features => {
        return features.filter(f => !ignoreFeatures.has(f.id));
      }));
    }
  });
  const features = (await Promise.all(promises)).flat();
  return features;
}