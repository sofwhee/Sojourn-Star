<%= stylesheet_link_tag "layouts/admins" %>

<%= render "edit_form", page: @page%>

<%= link_to "Delete", page_path(@page), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "adminbutton" %>