<h1>Edit page</h1>

<%= render "form", page: @page%>
<%= link_to "Destroy", page_path(@page), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>