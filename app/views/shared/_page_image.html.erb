<% if @page %>
<%= link_to_if(
  @page.next, 
  image_tag(url_for(@page.page_image), 
  id: "cimage", 
  size: "900x1266", 
  alt: "An arrow pointing left with a line next to it. It directs you to the first comic page."),
  @page.next,
  class: "comicpage") %>
<% end %>